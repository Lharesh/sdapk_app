import 'package:flutter/material.dart';

class ListTherapistsScreen extends StatelessWidget {
  const ListTherapistsScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Therapists to be added'));
  }
}
